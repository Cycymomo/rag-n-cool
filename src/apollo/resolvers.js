//import { gql } from 'apollo-boost'

// @TODO : find a way to trigger token here ?
export const defaults = {
  isConnected: false,
}

export const resolvers = {
  Mutation: {
    // loggedInUser: (_, { id }, { cache, getCacheKey }) => {
    //   return id
    // },
  },
}
